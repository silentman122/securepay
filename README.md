# securepay
This code creates a web app that predicts if a transaction is fraudulent. Users enter details like amount, time, country, and frequency. The app uses a trained machine learning model to analyze the data and tells you if the transaction is safe or suspicious, showing the probability. It has a simple, styled interface and runs on a free server port. 
